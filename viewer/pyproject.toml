[project]
name = "trace-viewer"
version = "0.1.0"
description = "Web viewer for LLM thinking trace data"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "jinja2>=3.1",
]
